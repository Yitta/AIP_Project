import { EmailFormComponent } from './email-form.component';

import { Route } from '@angular/router';

export const EmailFormRoute: Route[] = [
    {
        path: 'email-form',
        component: EmailFormComponent
    }
];